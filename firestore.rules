rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // All access via the Worker (service account bypasses rules)
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
